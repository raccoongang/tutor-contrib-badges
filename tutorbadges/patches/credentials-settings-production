BADGES_CONFIG['credly']['USE_SANDBOX'] = {% if BADGES_CREDLY_USE_SANDBOX %}True{% else %}False{% endif %}
BADGES_CONFIG.get('accredible', {'USE_SANDBOX': False})['USE_SANDBOX'] = {% if BADGES_ACCREDIBLE_USE_SANDBOX %}True{% else %}False{% endif %}
